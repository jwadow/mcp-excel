<div align="center">

# 📊 Excel MCP Server

**Быстрый и эффективный анализ таблиц через атомарные операции, созданный специально для ИИ-агентов**

[🇬🇧 English](../../README.md) • 🇷🇺 Русский • [🇨🇳 中文](../zh/README.md) • [🇪🇸 Español](../es/README.md) • [🇯🇵 日本語](../ja/README.md) • [🇧🇷 Português](../pt/README.md)

Сделано с ❤️ от [@Jwadow](https://github.com/jwadow)

[![License: AGPL v3](https://img.shields.io/badge/License-AGPL%20v3-blue.svg)](https://www.gnu.org/licenses/agpl-3.0)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![MCP](https://img.shields.io/badge/MCP-Compatible-green.svg)](https://modelcontextprotocol.io)
[![Sponsor](https://img.shields.io/badge/💖_Поддержать-Развитие_проекта-ff69b4)](#-поддержать-проект)

**Анализируйте Excel-таблицы с помощью ИИ-агента через атомарные операции — без загрузки данных в контекст**

*Работает с OpenCode, Claude Code, Codex app, Cursor, Cline, Roo Code, Kilo Code и другими MCP-совместимыми ИИ-агентами*

[Зачем это нужно](#-зачем-это-нужно) • [Что умеет ваш агент](#-что-умеет-ваш-агент) • [Установка и настройка](#%EF%B8%8F-установка-и-настройка) • [Доступные инструменты](#%EF%B8%8F-доступные-инструменты) • [💖 Поддержать](#-поддержать-проект)

</div>

---

## 🤨 Зачем это нужно

**Проблема:** Большинство Excel-инструментов для ИИ просто сваливают сырые данные таблицы в контекст агента. Это забивает контекстное окно, всё тормозит, а ИИ всё равно может ошибиться в расчётах или запутаться в больших датасетах.

**Этот проект:** Представьте SQL для Excel. Ваш ИИ-агент комбинирует атомарные операции (`filter_and_count`, `aggregate`, `group_by`) и получает точные результаты — а не тысячи строк.

Агент анализирует данные **не видя их**. Результаты приходят в виде чисел, формул и инсайтов.

> *"Это как работать с базой данных через SQL, а не тащить всё в память."*
> — ИИ-агент после анализа продакшн-таблицы

### 🔌 Что такое MCP?

[Model Context Protocol](https://modelcontextprotocol.io) — это открытый стандарт, который позволяет ИИ-агентам использовать внешние инструменты.

Этот проект — как раз такой инструмент. Когда вы подключаете этот сервер к своему ИИ-агенту (OpenCode, Claude Code, Codex app, Cursor, Cline, Roo Code, Kilo Code и т.д.), ваш агент получает кучу новых команд для работы с Excel-файлами — фильтрация, подсчёт, агрегация, анализ.

**Главное преимущество:** Ваш ИИ не загружает тысячи строк таблицы в свою память. Вместо этого он задаёт конкретные вопросы и получает точные ответы. Быстрее, точнее, без переполнения контекста.

---

## 💬 Что говорят ИИ-агенты

Реальные отзывы от ИИ-агентов, которые использовали этот MCP-сервер в продакшне:

> *"Проанализировал 34 211 строк без загрузки данных в контекст. Каждая операция возвращает только результат — количество, сумму, среднее. Контекст остаётся чистым. Операции выполняются за 25-45 мс независимо от размера файла."*

> *"Это SQL для Excel. Запросы, фильтры, агрегация — без сваливания данных в контекст. Годный инструмент для аналитических задач."*

> *"Система фильтров хорошо справляется со сложной логикой. Вложенные AND/OR группы, 12 операторов, неограниченное количество условий. Построил мультикатегориальную классификацию без написания кода."*

> *"Пакетные операции эффективны. Один вызов `filter_and_count_batch` вместо множества отдельных запросов. Файл загружается один раз, все фильтры применяются, результаты приходят вместе."*

*Да, агенты теперь пишут отзывы. Это реальные размышления ИИ-агентов, анализирующих реальные табличные данные. Добро пожаловать в 2026.*

---

## 🚀 Что умеет ваш агент

После подключения ваш ИИ-агент получает кучу специализированных инструментов для анализа табличных данных. Агент получает только точные запросы и надёжные результаты.

### 📊 Исследование данных
- **Инспекция файлов** - структура, листы, колонки, типы данных (автоопределение грязных заголовков)
- **Профилирование колонок** - статистика, количество null, топ значений, качество данных в одном вызове
- **Поиск данных** - поиск по нескольким листам, поиск колонок где угодно

### 🔍 Фильтрация и запросы
- **12 операторов фильтрации** - `==`, `!=`, `>`, `<`, `>=`, `<=`, `in`, `not_in`, `contains`, `startswith`, `endswith`, `regex`
- **Сложная логика** - вложенные AND/OR группы, оператор NOT, неограниченное количество условий
- **Пакетные операции** - классификация данных по нескольким категориям за один запрос (в 6 раз быстрее)
- **Анализ пересечений** - диаграммы Венна, подсчёт пересечений, операции над множествами

### 📈 Агрегация и анализ
- **8 функций агрегации** - sum, mean, median, min, max, std, var, count
- **Группировка** - сводные таблицы с несколькими колонками группировки
- **Статистический анализ** - корреляции (Pearson/Spearman/Kendall), обнаружение выбросов (IQR/Z-score)
- **Временные ряды** - рост период-к-периоду, скользящие средние, накопительные итоги

### 🏆 Продвинутые операции
- **Ранжирование** - топ-N, нижние-N, процентильное ранжирование (с поддержкой группировки)
- **Вычисляемые колонки** - арифметические выражения между колонками
- **Валидация данных** - поиск дубликатов, null-значений, проверки качества данных
- **Сравнение листов** - разница между версиями, поиск изменений

### ⚡ Производительность
- **Атомарные операции** - результаты за 20-50 мс, независимо от размера файла
- **Умное кеширование** - файл загружается один раз, переиспользуется для всех операций
- **Примеры строк** - предпросмотр отфильтрованных данных без полной выгрузки
- **Защита контекста** - умные лимиты предотвращают переполнение контекста ИИ

### 📋 Интеграция с Excel
- **Генерация формул** - каждый результат включает Excel-формулу для динамических обновлений
- **TSV-вывод** - копируйте-вставляйте результаты прямо в Excel
- **Поддержка legacy** - работает со старыми .xls файлами (Excel 97-2003)
- **Мультилист** - анализ по нескольким листам в одном файле

**Примеры запросов, которые теперь понимает ваш агент:**
- *"Покажи топ-10 клиентов по выручке"*
- *"Найди все заказы из 4 квартала, где сумма > 1000₽"*
- *"Посчитай рост месяц-к-месяцу для каждой категории товаров"*
- *"Какие клиенты одновременно VIP и активные? (анализ пересечений)"*
- *"Найди дубликаты в колонке Email"*

## ⚙️ Установка и настройка

### Требования

**Python 3.10 или выше** — [Скачать здесь](https://www.python.org/downloads/)

### Шаг 1: Клонировать репозиторий

```bash
git clone https://github.com/jwadow/mcp-excel.git
cd mcp-excel
```

*Нет Git? Нажмите "Code" → "Download ZIP" вверху страницы репозитория, распакуйте и откройте терминал в этой папке.*

### Шаг 2: Выбрать способ установки

<details>
<summary><b>🎯 Вариант A: Poetry (Рекомендуется)</b></summary>

Poetry — это современный менеджер зависимостей Python (заменяет pip+venv+requirements.txt).
[Установите его](https://python-poetry.org/docs/#installation): `pip install poetry` или `pipx install poetry`

**Установить зависимости:**
```bash
poetry install
```

**Настроить ИИ-агента:**

Добавьте это в настройки MCP (JSON-конфиг):
```json
{
  "mcpServers": {
    "excel": {
      "command": "poetry",
      "args": ["run", "python", "-m", "mcp_excel.main"],
      "cwd": "C:/path/to/mcp-excel"
    }
  }
}
```

**Важно:** Замените `C:/path/to/mcp-excel` на реальный путь к клонированному репозиторию.

</details>

<details>
<summary><b>📦 Вариант B: pip с виртуальным окружением</b></summary>

**Установить зависимости:**
```bash
# Windows
python -m venv venv
venv\Scripts\activate
pip install -e .

# Linux/Mac
python -m venv venv
source venv/bin/activate
pip install -e .
```

**Найти путь к Python в venv:**
```bash
# Windows
where python

# Linux/Mac
which python
```

**Настроить ИИ-агента:**

Добавьте это в настройки MCP (JSON-конфиг):
```json
{
  "mcpServers": {
    "excel": {
      "command": "C:/path/to/mcp-excel/venv/Scripts/python.exe",
      "args": ["-m", "mcp_excel.main"],
      "cwd": "C:/path/to/mcp-excel"
    }
  }
}
```

**Важно:**
- Замените `C:/path/to/mcp-excel/venv/Scripts/python.exe` на реальный путь из команды `where python`
- На Linux/Mac используйте путь из `which python` (например, `/path/to/mcp-excel/venv/bin/python`)

</details>

<details>
<summary><b>🐍 Вариант C: Системный Python (Не рекомендуется)</b></summary>

**Установить зависимости глобально:**
```bash
pip install "mcp>=1.1.0" "pandas>=2.2.0" "pydantic>=2.10.0" "xlrd>=2.0.1" "openpyxl>=3.1.0" "psutil>=6.1.0" "python-dateutil>=2.9.0"
```

**Настроить ИИ-агента:**
```json
{
  "mcpServers": {
    "excel": {
      "command": "python",
      "args": ["-m", "mcp_excel.main"],
      "cwd": "C:/path/to/mcp-excel"
    }
  }
}
```

⚠️ **Предупреждение:** Это засоряет глобальное Python-окружение. Используйте Poetry или venv вместо этого.

</details>

### Шаг 3: Проверить установку

Перезапустите ИИ-агента и протестируйте:
```
"Проанализируй Excel-файл по пути C:/Users/YourName/Documents/test.xlsx"
```

Если работает — готово! Если нет, проверьте:
- Путь к репозиторию правильный в `cwd`
- Путь к Python правильный в `command` (для метода pip)
- Все зависимости установлены

### Поддерживаемые ИИ-агенты

Работает с любым MCP-совместимым ИИ-агентом.

⚠️ **Важно:** Это MCP-сервер. Он запускается автоматически, когда вашему ИИ-агенту это нужно. Не запускайте его вручную в терминале.

## 💡 Использование

После настройки перезапустите ИИ-агента и попросите его проанализировать Excel-файлы:

```
"Проанализируй Excel-файл по пути C:/Users/YourName/Documents/sales.xls"
"Покажи топ-10 клиентов по выручке из sales.xlsx"
"Найди дубликаты в колонке 'Email' в contacts.xlsx"
"Посчитай рост месяц-к-месяцу из revenue.xls"
```

## 🛠️ Доступные инструменты

<details>
<summary><b>📋 Полный справочник инструментов (25 инструментов) - Нажмите, чтобы развернуть</b></summary>

### 📊 Инспекция файлов (5 инструментов)

#### `inspect_file`
Получить обзор структуры файла — листы, размеры, формат.
**Используйте для:** Первичного исследования файла, обнаружения листов, валидации формата
**Возвращает:** Список листов, количество строк/колонок, метаданные файла

#### `get_sheet_info`
Детальный анализ листа с автоопределением заголовков.
**Используйте для:** Понимания структуры данных, типов колонок, предпросмотра примеров
**Возвращает:** Имена/типы колонок, количество строк, примеры данных (3 строки), информацию об определении заголовков

#### `get_column_names`
Быстрое перечисление колонок без загрузки полных данных.
**Используйте для:** Валидации схемы, построения фильтров, проверки доступности колонок
**Возвращает:** Список имён колонок, количество колонок

#### `get_data_profile`
Комплексное профилирование колонок — типы, статистика, null, топ значений.
**Используйте для:** Первичного исследования данных, оценки качества, анализа распределения
**Возвращает:** Для каждой колонки: тип, % null, количество уникальных, статистика (числовые), топ N значений
**Эффективность:** Заменяет 10+ отдельных вызовов (get_column_stats + get_value_counts + find_nulls)

#### `find_column`
Найти колонку по нескольким листам.
**Используйте для:** Навигации по мультилистам, обнаружения данных, кросс-листового анализа
**Возвращает:** Список листов с расположением колонки, индексами, количеством строк (без учёта регистра)

---

### 📥 Получение данных (3 инструмента)

#### `get_unique_values`
Извлечь уникальные значения из колонки.
**Используйте для:** Исследования данных, построения фильтров, обнаружения уникальных значений, проверки качества данных
**Возвращает:** Список уникальных значений, количество, флаг обрезки (если превышен лимит)
**Лимит по умолчанию:** 100 значений

#### `get_value_counts`
Частотный анализ — топ N самых частых значений.
**Используйте для:** Анализа распределения, определения доминирующих категорий, обнаружения дисбаланса данных
**Возвращает:** Словарь значение → количество, общее количество, TSV-вывод
**По умолчанию:** Топ-10 значений

#### `filter_and_get_rows`
Получить отфильтрованные строки с пагинацией.
**Используйте для:** Извлечения данных, инспекции примеров, детального анализа, экспорта
**Возвращает:** Отфильтрованные строки (список словарей), общее количество, TSV-вывод
**Пагинация:** Поддержка limit/offset

---

### 🔍 Фильтрация и подсчёт (3 инструмента)

#### `filter_and_count`
Подсчитать строки, соответствующие условиям, с 14 операторами.
**Операторы:** `==`, `!=`, `>`, `<`, `>=`, `<=`, `in`, `not_in`, `contains`, `startswith`, `endswith`, `regex`, `is_null`, `is_not_null`
**Логика:** Вложенные AND/OR группы, оператор NOT, неограниченное количество условий
**Используйте для:** Классификации, сегментации, валидации данных, подсчёта категорий
**Возвращает:** Количество + Excel-формула (COUNTIFS), опциональные примеры строк

#### `filter_and_count_batch`
Классифицировать данные по нескольким категориям за один вызов (в 6 раз быстрее).
**Используйте для:** Мультикатегориальной классификации, сегментации рынка, контроля качества
**Возвращает:** Количество + формула для каждой категории, TSV-таблица для Excel
**Эффективность:** Загружает файл один раз, применяет все фильтры, возвращает все результаты

#### `analyze_overlap`
Анализ диаграммы Венна — пересечения, объединения, эксклюзивные зоны.
**Используйте для:** Анализа пересечений, кросс-продаж, проверки согласованности данных
**Возвращает:** Количества множеств, попарные пересечения (A ∩ B), объединение, данные Венна (2-3 множества)
**Примеры:** VIP И активные клиенты, пересечения категорий товаров, завершённые заказы БЕЗ даты завершения

---

### 📈 Агрегация и анализ (2 инструмента)

#### `aggregate`
Выполнить агрегацию с опциональными фильтрами (8 операций).
**Операции:** `sum`, `mean`, `median`, `min`, `max`, `std`, `var`, `count`
**Используйте для:** Итогов, средних, мин/макс значений, статистических сводок, условных агрегаций, расчёта KPI
**Возвращает:** Агрегированное значение + Excel-формула (SUMIF, AVERAGEIF и т.д.)
**Особенность:** Автоконвертация текстовых чисел в числовые

#### `group_by`
Сводная таблица с группировкой по нескольким колонкам.
**Используйте для:** Анализа категорий, иерархической группировки, продаж по регионам/товарам
**Возвращает:** Сгруппированные данные с агрегированными значениями, TSV-вывод
**Поддерживает:** Несколько колонок группировки, все 8 операций агрегации

---

### 📊 Статистика (3 инструмента)

#### `get_column_stats`
Статистическая сводка — количество, среднее, медиана, стандартное отклонение, квартили.
**Используйте для:** Анализа распределения, профилирования данных, подготовки к обнаружению выбросов
**Возвращает:** Полная статистика (min, max, mean, median, std, Q1, Q3), количество null, TSV-вывод

#### `correlate`
Матрица корреляций между 2+ колонками.
**Методы:** Pearson (линейная), Spearman (ранговая), Kendall (ранговая)
**Используйте для:** Анализа взаимосвязей, зависимости переменных, выбора признаков
**Возвращает:** Матрица корреляций (от -1 до 1), TSV-вывод

#### `detect_outliers`
Обнаружение аномалий методом IQR или Z-score.
**Методы:** IQR (устойчивый), Z-score (предполагает нормальное распределение)
**Используйте для:** Обнаружения мошенничества, ошибок датчиков, качества данных, необычных значений
**Возвращает:** Строки-выбросы с индексами, количество, использованный метод/порог

---

### ✅ Валидация данных (2 инструмента)

#### `find_duplicates`
Обнаружить дублирующиеся строки по указанным колонкам.
**Используйте для:** Качества данных, планирования дедупликации, проверки целостности
**Возвращает:** Все дублирующиеся строки (включая первое вхождение), количество, индексы
**Примечание:** Использует `duplicated(keep=False)` для пометки всех дубликатов

#### `find_nulls`
Найти null/пустые значения с детальной статистикой.
**Используйте для:** Проверки полноты, анализа пропущенных значений, очистки данных
**Возвращает:** Для каждой колонки: количество null, процент, индексы (первые 100)
**Примечание:** Плейсхолдеры (".", "-") НЕ являются null — используйте операторы `==` или `in`

---

### 🔄 Операции с несколькими листами (2 инструмента)

#### `search_across_sheets`
Поиск значения по всем листам.
**Используйте для:** Кросс-листового поиска, отслеживания значений, поиска данных
**Возвращает:** Список листов с количеством совпадений, общее количество совпадений
**Поддерживает:** Числовые и строковые значения

#### `compare_sheets`
Разница между двумя листами с использованием ключевой колонки.
**Используйте для:** Сравнения версий, обнаружения изменений, сверки, аудита
**Возвращает:** Строки с различиями, статус (only_in_sheet1/sheet2/different_values), сравнение бок-о-бок

---

### 📅 Временные ряды (3 инструмента)

#### `calculate_period_change`
Анализ роста период-к-периоду.
**Периоды:** month, quarter, year
**Используйте для:** Анализа трендов, отслеживания роста, сезонного сравнения, YoY-анализа
**Возвращает:** Периоды со значениями, абсолютные/процентные изменения, Excel-формула

#### `calculate_running_total`
Накопительная сумма с опциональной группировкой.
**Используйте для:** Кумулятивного анализа, отслеживания прогресса, расчёта балансов, денежного потока
**Возвращает:** Строки с накопительными итогами, Excel-формула (SUM($B$2:B2))
**Поддерживает:** Группировку (накопительный итог сбрасывается для каждой группы)

#### `calculate_moving_average`
Сглаживание с указанным размером окна.
**Используйте для:** Обнаружения трендов, снижения шума, идентификации паттернов
**Возвращает:** Строки со скользящими средними, Excel-формула (AVERAGE(B1:B7))
**Примеры:** 7-дневное скользящее среднее, 30-дневное сглаживание цены акций

---

### 🏆 Продвинутые операции (2 инструмента)

#### `rank_rows`
Ранжирование по значению колонки с фильтрацией топ-N.
**Направления:** desc (сначала наибольшие), asc (сначала наименьшие)
**Используйте для:** Лидербордов, топ/нижний анализ, процентильного ранжирования
**Возвращает:** Ранжированные строки с номерами рангов, Excel-формула (RANK)
**Поддерживает:** Фильтрацию топ-N, ранжирование внутри групп

#### `calculate_expression`
Арифметические выражения между колонками.
**Операции:** `+`, `-`, `*`, `/`, скобки
**Используйте для:** Производных метрик, финансовых расчётов, анализа коэффициентов, расчёта KPI
**Возвращает:** Вычисленные значения, Excel-формула (например, =A2*B2)
**Примеры:** Выручка = Цена * Количество, Маржа = (Выручка - Затраты) / Выручка

</details>

## 🗺️ Дорожная карта

### 📁 Поддержка форматов файлов

**Сейчас поддерживается:**
- ✅ **XLS** - Excel 97-2003 (только чтение)
- ✅ **XLSX** - Excel 2007+ (только чтение)

**В планах:**
- 🔜 **XLSM** - Excel с поддержкой макросов
- 🔜 **CSV** - Значения, разделённые запятыми
- 🔜 **TSV** - Значения, разделённые табуляцией
- 🔜 **ODS** - OpenDocument Spreadsheet
- 🔜 **Parquet** - Колоночный формат хранения

### 🚀 Функции

- **Операции записи** - Изменение файлов таблиц (создание вычисляемых колонок, обновление значений)
- **Режим SSE-транспорта** - Server-Sent Events для удалённого доступа
- **Продвинутая генерация формул** - Более сложные Excel-формулы с вложенными функциями
- **Экспорт данных** - Экспорт отфильтрованных/агрегированных результатов в новые файлы

---

## 📜 Лицензия

Этот проект распространяется под лицензией **GNU Affero General Public License v3.0 (AGPL-3.0)**.

Это означает:
- ✅ Вы можете использовать, изменять и распространять это ПО
- ✅ Вы можете использовать его в коммерческих целях
- ⚠️ **Вы должны раскрывать исходный код** при распространении ПО
- ⚠️ **Сетевое использование = распространение** — если вы запускаете модифицированную версию на сервере и даёте другим взаимодействовать с ней, вы должны предоставить исходный код
- ⚠️ Модификации должны распространяться под той же лицензией

Полный текст лицензии см. в файле [LICENSE](../../LICENSE).

### Почему AGPL-3.0?

AGPL-3.0 гарантирует, что улучшения этого ПО приносят пользу всему сообществу. Если вы модифицируете этот сервер и разворачиваете его как сервис, вы должны делиться своими улучшениями с пользователями.

---

## 💖 Поддержать проект

<div align="center">

<img src="https://raw.githubusercontent.com/Tarikul-Islam-Anik/Animated-Fluent-Emojis/master/Emojis/Smilies/Smiling%20Face%20with%20Hearts.png" alt="Love" width="80" />

**Если этот проект сэкономил вам время или деньги, подумайте о его поддержке!**

Каждый вклад помогает поддерживать проект живым и растущим

<br>

### 🤑 Донат

[**☕ Разовый донат**](https://app.lava.top/jwadow?tabId=donate) • [**💎 Ежемесячная поддержка**](https://app.lava.top/jwadow?tabId=subscriptions)

<br>

### 🪙 Или отправьте крипту

| Валюта | Сеть | Адрес |
|:--------:|:-------:|:--------|
| **USDT** | TRC20 | `TSVtgRc9pkC1UgcbVeijBHjFmpkYHDRu26` |
| **BTC** | Bitcoin | `12GZqxqpcBsqJ4Vf1YreLqwoMGvzBPgJq6` |
| **ETH** | Ethereum | `0xc86eab3bba3bbaf4eb5b5fff8586f1460f1fd395` |
| **SOL** | Solana | `9amykF7KibZmdaw66a1oqYJyi75fRqgdsqnG66AK3jvh` |
| **TON** | TON | `UQBVh8T1H3GI7gd7b-_PPNnxHYYxptrcCVf3qQk5v41h3QTM` |

</div>

---

## 🤝 Участие в разработке

Приветствуются любые вклады! Пожалуйста, убедитесь, что:

1. Все зависимости совместимы с AGPL
2. Код следует существующему стилю
3. Тесты включены для новых функций
4. Документация обновлена

По вопросам, багам или предложениям открывайте issue на GitHub.

---

## 💬 Нужна помощь?

Есть вопросы? Нашли баг? Есть идея для новой функции? Мы здесь, чтобы помочь!

**👉 [Открыть Issue на GitHub](https://github.com/jwadow/mcp-excel/issues/new)**

Застряли с установкой, нашли что-то сломанное или просто хотите предложить улучшение — GitHub Issues — это то место. Не волнуйтесь, если вы новичок в GitHub, просто нажмите на ссылку выше и опишите свою ситуацию. Мы разберёмся вместе.

---

<div align="center">

**[⬆ Наверх](#-excel-mcp-server)**

</div>
